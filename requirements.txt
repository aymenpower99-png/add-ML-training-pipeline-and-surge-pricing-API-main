# ── Core ──
numpy
pandas

# ── Machine Learning ──
scikit-learn
xgboost
lightgbm
joblib

# ── API (Pricing API — FastAPI) ──
fastapi
uvicorn[standard]
pydantic

# ── Config API (Flask) ──
flask
flask-cors
gunicorn

# ── Database ──
psycopg2-binary
python-dotenv

# ── Data / HTTP ──
requests
requests-cache
retry-requests
python-dateutil

# ── Utils ──
tqdm

# ── Optional (viz/debug) ──
matplotlib
seaborn
openmeteo-requests
